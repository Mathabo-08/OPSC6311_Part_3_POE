<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Required for Firebase to work -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/zenith_logo"
        android:background="@color/YELLOW"
        android:label="@string/app_name"
        android:roundIcon="@drawable/zenith_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Zenith"
        tools:targetApi="31">


    <activity
            android:name=".Welcome"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".Reports"
            android:exported="true">
    </activity>

        <activity
            android:name=".GameficationRewards"
            android:exported="true">
        </activity>

        <activity
            android:name=".FinancialLit"
            android:exported="true">
        </activity>

        <activity
            android:name=".SpendingSum"
            android:exported="true">
        </activity>

        <activity
            android:name=".QuickExpenses"
            android:exported="true">
        </activity>

        <activity
            android:name=".Goals"
            android:exported="true">
        </activity>

        <activity
            android:name=".MonthlyBudget"
            android:exported="true">
        </activity>

        <activity
            android:name=".Home"
            android:exported="true">
        </activity>

        <activity
            android:name=".Register"
            android:exported="true">
        </activity>

        <activity
            android:name=".Login"
            android:exported="true">
        </activity>

    </application>

</manifest>